apps:
  - script      : 'yarn'
    args        : 'start --port 8883'
    name        : 'hct-admin-new'
    exec_mode   : 'cluster'
    instances   : 'max'
    watch       : false
    ignore_watch: ['./node_modules', './tmp', './.nuxt', './.git']
  
